name: "Java CI"
on:
  push:
    branches:
      - '[4-9]+.[0-9]+.x'
  pull_request:
    branches:
      - '[4-9]+.[0-9]+.x'
jobs:
  test_project:
    name: "Test Project"
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: true
      matrix:
        java: [17, 21]
    steps:
      - name: "📥 Checkout repository"
        uses: actions/checkout@v4
      - name: "☕️ Setup JDK"
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: liberica
      - name: "🐘 Setup Gradle"
        uses: gradle/actions/setup-gradle@v4
        with:
          develocity-access-key: ${{ secrets.DEVELOCITY_ACCESS_KEY }}
      - name: "🏃 Run tests"
        run: ./gradlew check
      - name: "🏃 Run integration tests"
        working-directory: ./examples/testapp1
        run: ./gradlew integrationTest
  publish_snapshot:
    if: github.event_name == 'push'
    name: "Build Project and Publish Snapshot release"
    needs: test_project
    runs-on: ubuntu-24.04
    permissions:
      content: write # updates gh-pages branch
      packages: write # publishes snapshot to GitHub Packages
    steps:
      - name: "📥 Checkout repository"
        uses: actions/checkout@v4
      - name: "☕️ Setup JDK"
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: liberica
      - name: "🐘 Setup Gradle"
        uses: gradle/actions/setup-gradle@v4
        with:
          develocity-access-key: ${{ secrets.DEVELOCITY_ACCESS_KEY }}
      - name: "🔨 Build Project"
        run: ./gradlew build
      - name: "📤 Publish Snapshot version to Github Packages"
        env:
          GITHUB_MAVEN_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_PUBLISH_RELEASE: 'false'
          MAVEN_PUBLISH_USERNAME: ${{ secrets.GITHUB_ACTOR }}
          MAVEN_PUBLISH_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          MAVEN_PUBLISH_URL: 'https://maven.pkg.github.com/gpc/grails-mail'
        run: ./gradlew publish
      - name: "📖 Generate Snapshot Documentation"
        run: ./gradlew docs
      - name: "📤 Publish Snapshot Documentation to Github Pages"
        uses: apache/grails-github-actions/deploy-github-pages@asf
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_PUBLISH_RELEASE: 'false'
          SOURCE_FOLDER: build/docs